# Shared secret for auth (used by both server and client)
GHCP_SYNC_KEY=change-me

# Client: server URL
GHCP_SYNC_URL=http://localhost:3457

# Server: port to listen on (default: 3457)
GHCP_SYNC_PORT=3457

# Server: directory to store the config tarball (default: ./data)
GHCP_SYNC_DATA_DIR=./data
